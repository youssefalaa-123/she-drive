// Firebase removed — app now uses Supabase exclusively.
// This stub prevents crashes if any import was missed during migration.
export const auth      = null;
export const db        = null;
export const functions = null;
